<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/action_add"
        android:icon="@drawable/ic_add"
        app:showAsAction="always"
        android:title="@string/add"/>

    <item android:id="@+id/search_menu"
        android:title="@string/search_products"
        android:icon="@android:drawable/ic_menu_search"
        app:actionViewClass="android.support.v7.widget.SearchView"
        app:showAsAction="always"/>

    <item android:id="@+id/action_set_all_products_as_bought"
        android:title="Cross all products"
        app:showAsAction="never"/>

    <item android:id="@+id/action_move_crossed_products_to_virtual_fridge"
        android:title="Move crossed products to fridge"
        app:showAsAction="never"/>
</menu>